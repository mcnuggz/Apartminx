@model IEnumerable<Apartment.Models.Authorization>

@{
    ViewBag.Title = "Index";
}

<legend class="legend">@ViewBag.Title</legend>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AcceptTerms)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ESignature)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SpouseSignature)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AdditionalComments)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AcceptTerms)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ESignature)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SpouseSignature)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdditionalComments)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
